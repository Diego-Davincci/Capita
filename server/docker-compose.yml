version: "3.8"

services:
  db:
    image: postgres:16.3
    command: -p 5433
    container_name: bulletproof-monster-stack
    environment:
      POSTGRES_DB: monster-stack
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: root
    ports:
      - "5433:5433"

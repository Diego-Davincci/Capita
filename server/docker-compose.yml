version: "3.8"

services:
  db:
    image: postgres:16.3
    command: -p 5432
    container_name: capita-db
    environment:
      POSTGRES_DB: capita
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"
